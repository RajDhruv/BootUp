<%= paginate @latest_notifications, :remote => true %>
<%@latest_notifications.each do |notification|%>
	<%=render 'notifications/notification',notification:notification%>
<%end%>
<%= paginate @latest_notifications, :remote => true %>